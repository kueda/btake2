<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bee_id %><br />
    <%= f.number_field :bee_id %>
  </div>
  <div class="field">
    <%= f.label :retakeable %><br />
    <%= f.number_field :retakeable %>
  </div>
  <div class="field">
    <%= f.label :accessibility %><br />
    <%= f.number_field :accessibility %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
